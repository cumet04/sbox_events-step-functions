version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        USER_UID:
        USER_GID:
    init: true
    volumes:
      - ..:/workspace:cached
    command: sleep infinity

volumes:
  node_modules:
